{
  "name": "findeg-app",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "type": "module",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "dx": "docker compose up -d postgres && turbo run dev --filter=@findeg/storefront --filter=@findeg/dashboard",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "test": "turbo run test",
    "db:setup": "pnpm db:generate && pnpm db:push && pnpm db:seed",
    "db:studio": "docker compose up -d postgres && pnpm --filter @findeg/db db:studio",
    "clean": "turbo run clean && rm -rf node_modules .turbo",
    "clean:next": "rm -rf frontend/dashboard/.next frontend/storefront/.next"
  },
  "devDependencies": {
    "turbo": "^2.9.3",
    "typescript": "^5.7.3"
  }
}