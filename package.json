{
  "name": "findeg-app",
  "version": "1.0.0",
  "description": "FindEg.com E-commerce Application",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "turbo run dev --filter=@findeg/storefront --filter=@findeg/dashboard",
    "build": "turbo run build",
    "start": "turbo run start",
    "type-check": "turbo run type-check",
    "lint": "turbo run lint",
    "test": "turbo run type-check && turbo run test:unit && turbo run test:e2e",
    "test:unit": "turbo run test:unit",
    "test:e2e": "turbo run test:e2e",
    "dev:storefront": "pnpm --filter @findeg/storefront dev",
    "dev:dashboard": "pnpm --filter @findeg/dashboard dev",
    "dev:both": "pnpm --parallel --filter @findeg/storefront --filter @findeg/dashboard dev",
    "db:start": "./backend/scripts/db-start.sh",
    "db:stop": "./backend/scripts/db-stop.sh",
    "db:reset": "./backend/scripts/db-reset.sh",
    "db:logs": "./backend/scripts/db-logs.sh",
    "db:shell": "docker-compose exec postgres psql -U findeg_user -d findeg_dev",
    "db:status": "docker-compose ps && docker-compose exec postgres pg_isready -U findeg_user",
    "db:setup": "pnpm db:start && pnpm db:push && pnpm db:seed",
    "db:generate": "drizzle-kit generate --config backend/drizzle.config.ts",
    "db:migrate": "drizzle-kit migrate --config backend/drizzle.config.ts",
    "db:push": "drizzle-kit push --config backend/drizzle.config.ts --force",
    "db:seed": "node backend/scripts/seed-db.js",
    "db:seed-brands": "npx tsx --tsconfig backend/tsconfig.json backend/scripts/seed-brands.ts",
    "db:studio": "drizzle-kit studio --config backend/drizzle.config.ts",
    "db:doc": "node backend/scripts/generate-schema-doc.js"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.8.0",
    "start-server-and-test": "^3.0.0",
    "turbo": "^2.9.3",
    "typescript": "^5.7.3"
  },
  "keywords": [
    "react",
    "ecommerce",
    "tailwindcss",
    "nextjs"
  ],
  "author": "FindEg Team",
  "packageManager": "pnpm@10.33.0"
}