---
description: "Tasks for fixing storefront type errors"
---

# Tasks: Fix Storefront Type Errors

**Input**: Design documents from `/specs/003-fix-storefront-types/`
**Prerequisites**: plan.md, spec.md, research.md, data-model.md
**Constitution**: All tasks must satisfy FindEg.com Constitution principles.

## Constitution Compliance Tasks (Principle-Driven)

### V. Type-Safe & Testable Code

- [ ] T001 [P] Ensure strictly typed event handlers without 'any'
- [ ] T002 [P] Propagate proper nullability into application from external calls
- [ ] T003 Run full gate: `npm run type-check`

### VI. DRY Principle

- [ ] T004 [P] Ensure UI components are correctly exported from shared UI instead of recreating them

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: No setup required.

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Fix missing exports and dependencies that break the build environment completely.

- [x] T005 [P] Add `drizzle-orm`, `resend`, `swr`, `@react-email/components` to dependencies in `packages/storefront/package.json`
- [x] T006 Update `packages/ui/package.json` or `packages/ui/tsconfig.json` so that `dist` types are correctly emitted and module exports resolve.
- [x] T007 Run package manager install (`npm install`) to fetch the added dependencies.

**Checkpoint**: Core dependencies present, missing modules resolved.

## Phase 3: User Story 1 - Clean Build and Type-Check (Priority: P1) 🎯 MVP

**Goal**: Fix type mismatches in local storefront files.
**Independent Test**: Running `npm run type-check` will successfully exit with 0.

### Implementation for User Story 1

- [ ] T008 [P] [US1] Remove implicit 'any' in `packages/storefront/src/components/shared/SearchOverlay.tsx`
- [ ] T009 [P] [US1] Remove implicit 'any' in `packages/storefront/src/hooks/use-toast.ts`
- [ ] T010 [P] [US1] Fix nullability return constraint in `packages/storefront/src/queries/shop-queries.ts` (return `ProductDetailPageData | null`)

## Phase 4: User Story 2 - Correct UI Component Imports (Priority: P1)

**Goal**: Eradicate all `TS2305` missing export errors.
**Independent Test**: Opening `SearchOverlay.tsx` shows no module import squiggles.

### Implementation for User Story 2

- [ ] T011 [US2] Re-verify exports generated by `packages/ui` after T006, ensuring standard monorepo path mapping or build steps correctly surface `Button`, `Input`, etc.

## Phase N: Polish & Cross-Cutting Concerns

- [ ] T012 Run final `npm run type-check` locally from root repository and from `packages/storefront/`.

## Dependencies & Execution Order

- **Foundational (Phase 2)**: Resolves the package dependencies. Must be done first.
- **User Stories (Phase 3 & 4)**: Can run in parallel since US1 fixes code and US2 relies on foundational build output.

## Parallel Example: User Story 1

```bash
# Launch code changes for User Story 1 together:
Task: "Remove implicit 'any' in SearchOverlay.tsx"
Task: "Remove implicit 'any' in use-toast.ts"
Task: "Fix nullability return constraint in shop-queries.ts"
```

## Implementation Strategy

### MVP First

1. Complete Phase 2: Foundational (CRITICAL - blocks type checking)
2. Complete Phase 3: User Story 1 & Phase 4: User Story 2
3. **STOP and VALIDATE**: Verify all types check independently.
