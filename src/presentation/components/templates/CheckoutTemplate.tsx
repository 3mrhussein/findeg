import React from 'react';
import { CheckoutContent } from '@/presentation/components/features/shop/CheckoutContent';

const CheckoutTemplate: React.FC = () => {
    return <CheckoutContent />;
};

export default CheckoutTemplate;