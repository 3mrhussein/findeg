{
  "name": "@findeg/backend",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "test": "vitest"
  },
  "dependencies": {
    "zod": "^4.20.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^1.3.0"
  }
}
